/**
 * Payment System Constants
 */

export const PAYMENT_CONSTANTS = {
  // Performance targets
  TARGET_TPS: 1000,
  MAX_RESPONSE_TIME_MS: 100,
  
  // Batch processing
  DEFAULT_BATCH_SIZE: 100,
  MAX_BATCH_SIZE: 500,
  BATCH_TIMEOUT_MS: 1000,
  
  // Queue management
  MAX_QUEUE_SIZE: 10000,
  QUEUE_POLL_INTERVAL_MS: 50,
  PRIORITY_WEIGHTS: {
    low: 1,
    normal: 2,
    high: 5,
    critical: 10
  },
  
  // Retry configuration
  MAX_RETRIES: 3,
  RETRY_DELAYS_MS: [1000, 2000, 5000],
  
  // Fee configuration
  DEFAULT_FEE_PERCENTAGE: 0.3, // 0.3%
  MIN_FEE_WEI: BigInt('1000000000000000'), // 0.001 ETH
  
  // Webhook configuration
  WEBHOOK_TIMEOUT_MS: 5000,
  WEBHOOK_MAX_RETRIES: 3,
  
  // Escrow configuration
  DEFAULT_ESCROW_DURATION_MS: 7 * 24 * 60 * 60 * 1000, // 7 days
  MIN_ESCROW_DURATION_MS: 60 * 60 * 1000, // 1 hour
  MAX_ESCROW_DURATION_MS: 90 * 24 * 60 * 60 * 1000, // 90 days
  
  // Supported tokens (addresses would be environment-specific)
  SUPPORTED_TOKENS: {
    ETH: 'native',
    USDC: '0xA0b86991c6218b36c1d19D4a2e9Eb0cE3606eB48',
    USDT: '0xdAC17F958D2ee523a2206206994597C13D831ec7',
    DAI: '0x6B175474E89094C44Da98b954EedeAC495271d0F',
    WETH: '0xC02aaA39b223FE8D0A0e5C4F27eAD9083C756Cc2'
  },
  
  // Gas configuration
  GAS_LIMIT_MULTIPLIER: 1.2,
  MAX_GAS_PRICE_GWEI: 500,
  
  // Metrics collection
  METRICS_WINDOW_SIZE: 1000,
  METRICS_UPDATE_INTERVAL_MS: 1000
} as const;

export const ERROR_CODES = {
  INSUFFICIENT_BALANCE: 'INSUFFICIENT_BALANCE',
  INVALID_TOKEN: 'INVALID_TOKEN',
  QUEUE_FULL: 'QUEUE_FULL',
  RATE_LIMIT_EXCEEDED: 'RATE_LIMIT_EXCEEDED',
  INVALID_RECIPIENT: 'INVALID_RECIPIENT',
  ESCROW_NOT_FOUND: 'ESCROW_NOT_FOUND',
  ESCROW_CONDITIONS_NOT_MET: 'ESCROW_CONDITIONS_NOT_MET',
  WEBHOOK_DELIVERY_FAILED: 'WEBHOOK_DELIVERY_FAILED',
  BATCH_PROCESSING_FAILED: 'BATCH_PROCESSING_FAILED',
  TRANSACTION_FAILED: 'TRANSACTION_FAILED'
} as const;